vim.keymap.set('n', '<space>e', vim.diagnostic.open_float)

lspconfig.rust_analyzer.setup {
  settings = {
    ['rust-analyzer'] = {
      diagnostics = {
        enable = true;
      }
    },
  },
}
